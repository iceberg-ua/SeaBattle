@using SeaBattle.Shared.Player

<lable class="mb-5">
    <b>@Names</b>
</lable>
<br />

@* @if (!IsStarted)
{
    <label>Welcome to the Sea Battle</label>

    @if (!WaitingOpponent)
    {
        <div class="text-center mb-1">
            Now postion your ships and press <b>Ready</b> button
        </div>
    }
}
else
{
    if (_gameIsOver)
    {
        <div class="text-center mb-1 @_gameOverClass">
            <h2>@_gameOverString</h2>
        </div>
    }
    else
    {
        <div class="text-center mb-1">
            Battle is on!
        </div>

        <div class="text-center mb-1">
            @if (WaitingForShot)
            {
                <b>It is your shot</b>
            }
            else
            {
                <b>Waitng for oponent's shot</b>
            }
        </div>
    }
} *@

@code {
    [CascadingParameter]
    public required GameStateClient GameState { get; set; }

    private string Names => GameState.Player?.Name + (string.IsNullOrEmpty(GameState.OpponentsName) ? "" : $"VS {GameState.OpponentsName}");
}
